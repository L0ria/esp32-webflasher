# Development-only test dependencies (issue #26, PR 1 — backend unit suite).
# These are NOT installed in the slim Docker app image; they are only needed to
# run the test suite.  Install with:  pip install -r requirements-dev.txt
#
# Runtime deps live in requirements.txt (flask, pinned).  Do not add runtime
# dependencies here.
pytest==8.3.5
# PR 3 (API integration suite): HTTP client used by tests/integration to talk
# to the real app.py server over loopback.  Dev-only.
requests==2.34.2
# Static-checks suite (issue #11, spec §9.5): parses docker-compose.yaml to
# validate its syntax/structure without Docker.  Dev-only.
PyYAML==6.0.3
