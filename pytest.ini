[pytest]
# Backend unit suite (issue #26, PR 1).  Run with:  pytest tests/unit
# (PR 3 will add tests/integration; run it explicitly with: pytest tests/integration)
testpaths = tests/unit
addopts = -ra --strict-markers
